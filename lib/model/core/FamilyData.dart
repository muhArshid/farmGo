class FamilyData {
  static int mother = 1;
  static int father = 2;
  static int child = 3;

  var id, img, imgType, name, type, selectedId;

  FamilyData(
      {this.id, this.img, this.imgType, this.name, this.type, this.selectedId});
}
